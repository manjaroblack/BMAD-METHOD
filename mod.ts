/**
 * Public API entry for BMAD-METHOD.
 *
 * This file exists to serve as a stable entry point for API documentation
 * generation (deno doc --lint/html/json). Re-export public symbols here as the
 * project evolves.
 *
 * @module
 */

/** Current project version. */
export const VERSION = '0.1.0' as const;
